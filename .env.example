# Copia a .env.development para desarrollo local
VITE_API_URL=http://127.0.0.1:5219

# En Netlify: dejar VITE_API_URL vacío (ya está en netlify.toml).
# El proxy de Netlify envía /login y /api al backend desplegado.

# Solo si NO usas proxy de Netlify (puede fallar por CORS o mixed content):
# VITE_API_URL=http://investigation2prograiv.runasp.net
